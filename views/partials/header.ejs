<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <h1 class="navbar-brand">TinyLinks</h1>
    <button
      class="navbar-toggler"
      type="button"
      data-toggle="collapse"
      data-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav d-flex justify-content-between w-100">
        <div class="d-flex">
          <a class="nav-item nav-link" href="/urls">My URLs</a>
          <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
        </div>
        <div class="d-flex">
          <% if(user){ %>
            <span class="nav-item nav-link"><%= user ? `Welcome ${user.name}!! You are logged in.` : ''; %></span>
            <form action="/auth/logout" method="post">
              <button type="submit" class='<%= user? "btn btn-danger" :''; %>'><%= user? 'Logout': ''; %></button>
            </form>
          <% }else{ %>
            <a class="nav-item nav-link" href="/auth/login"><%= user? '': 'Login'; %></a>
            <a class="nav-item nav-link" href="/auth/register"><%= user? '': 'Register'; %></a>
          <% } %>
        </div>
      </div>
    </div>
  </nav>
</header>
